# 價格路徑狀態持久化計畫

## 目標與邊界

本計畫把 App 已顯示的 20 日價格路徑圖示改為逐筆 `Trade` 持久狀態，讓 List、Trade 與技術檢視在 App 重啟或捲動畫面時直接讀取結果，不再為顯示而重播整段歷史。狀態只使用當日及以前的正式收盤價；Yahoo 同日價格只覆寫當日試算，不能把查詢次數當成新交易日。它是可在當日決定的滾動狀態，不是[未來價格路徑研究](未來價格路徑與技術數值組合研究計畫.md#未來價格路徑標籤)使用後續 20 日價格才建立的結果標籤。

價格路徑是行情描述，不是 Grade，也不直接增加或扣除 H買、L買、賣出或加碼分數。`RP-H01／H02／H04／H05／S01` 的 Sample A 反證只否定相應候選，不否定圖示與前後期切割作為 UI 和研究資料。若未來要把任一階段納入規則，仍須另立單一變因候選並完整重播。

正式導入依[版本與資料重算](版本與資料重算.md)將技術資料版本由 `T2` 推進為 `T3`，模擬資料規則維持 `S39`。PP-T3-08 已發布 T3 App，PP-T3-09 已完成集中資料池、A～E 分片、Baseline v21 與 DecisionBase v7 更新。

## 波動門檻

以最近最多 60 個相鄰正式收盤價的對數報酬計算樣本標準差，至少需要 40 筆報酬。20 日波動門檻為：

```text
b = clamp(sampleSigma(logReturn) × sqrt(20), 0.05, 0.15)
```

每個新區段建立時凍結當時的 `b`，區段內不因後續波動重新縮放。盤整超過正負 `b` 才建立探頂或探底；趨勢區段連續 20 個正式交易日沒有新極值時回到盤整。無效價格或日期不遞增會中斷序列，重新累積暖機資料。

## 穩定 raw value

`tPricePathPhaseRaw` 預定使用下列穩定整數值；未來新增狀態只能追加，不重新解釋既有值。

| Raw value | 程式階段 | 顯示名稱 | 主圖示 | 色彩強度 |
|---:|---|---|---|---|
| 0 | `unavailable` | 不可用／未暖機 | 隱藏 | 不適用 |
| 1 | `sideways` | 盤整 | 隱藏 | 不適用 |
| 2 | `seekingPeakEarly` | 探頂前期 | 紅色實心右上箭頭 | 58% |
| 3 | `seekingPeakLate` | 探頂後期 | 紅色實心右上箭頭 | 100% |
| 4 | `pullingBackEarly` | 拉回前期 | 紅色空心右下箭頭 | 58% |
| 5 | `pullingBackLate` | 拉回後期 | 紅色空心右下箭頭 | 100% |
| 6 | `seekingBottomEarly` | 探底前期 | 綠色實心右下箭頭 | 58% |
| 7 | `seekingBottomLate` | 探底後期 | 綠色實心右下箭頭 | 100% |
| 8 | `reboundingEarly` | 反彈前期 | 綠色空心右上箭頭 | 58% |
| 9 | `reboundingLate` | 反彈後期 | 綠色空心右上箭頭 | 100% |

前後期使用同一個主圖示與色相，以 58%／100% 色彩強度區分，不增加 List、橫式側欄或 Trade 的價格欄寬。直式與一般寬度版面保留圖示；緊湊橫式二欄的股群側欄及三欄的股群側欄／Trade 依版面規則省略圖示並收回占位，技術檢視不省略。彩色買賣底色上須保留不隨圖示淡化的高對比襯底；VoiceOver、help 與可存取文字直接說出完整階段，不只依賴顏色深淺。

## 前後期切割

四組狀態共用「相對於凍結門檻 `b` 的價格進度」方法，但主趨勢與轉折階段使用不同切點。

| 狀態 | 標準化進度 | 前期 | 後期 |
|---|---|---:|---:|
| 探頂 | `(runningHigh / anchorClose - 1) / b` | `1.0 <= p < 1.5` | `p >= 1.5` |
| 拉回 | `((runningHigh - close) / runningHigh) / b` | `0.5 <= p < 0.75` | `p >= 0.75` |
| 探底 | `((anchorClose - runningLow) / anchorClose) / b` | `1.0 <= p < 1.5` | `p >= 1.5` |
| 反彈 | `((close - runningLow) / runningLow) / b` | `0.5 <= p < 0.75` | `p >= 0.75` |

拉回或反彈達 `1.0b` 是轉入相反主趨勢的門檻。為維持「先轉折、再成為新趨勢」的連續性，狀態機每個交易日最多前進一個階段：若價格由探頂當日直接跨過 `1.0b` 回落，該日先記為拉回後期；下一個有效交易日仍符合時才成為探底。反彈轉探頂亦同。新高會由拉回回到探頂，新低會由反彈回到探底，舊日期不回改。

`1.5b` 曾用於 `RP-H05` 的 H買扣分候選並在 Sample A 退步；本計畫只把它固定為探頂／探底的顯示分段，不將該候選改寫成正式規則。`0.75b` 是拉回／反彈既有 `0.5b` 起轉與 `1.0b` 換向門檻的中點，同樣只描述階段進度。

## 2026/09/04 RP-S01：價格探頂後期時賣出加 1 分

RP-S01 的唯一變因是：持有庫存並評估賣出時，若個股決策當日價格路徑為 `seekingPeakLate`（探頂後期），賣出評分加 1；不限制大盤、Grade、Grade 趨勢、買入類型或持股天數。候選使用 Baseline v21 Sample A、`T3/S39`、600 萬元及兩次自動加碼，沿用既有 T3 技術值並從各窗口起點完整重播 `simUpdate`，不重算 `tUpdate`。13 項聚焦測試通過，確認九階段中只有探頂後期生效，候選旗標關閉時完全不作用。

18,864 次賣出評估中有 2,978 次直接加分，30 個股票窗口全部形成第一次分歧，最後改變 28 個窗口結果。總分由 `115.619` 降至 `114.119`（`-1.500`）；較弱股群 `+1.505`，較強股群 `-3.005`。六個窗口股群全部改變：最早窗口較弱／較強股群分別 `-5.053／-16.480`，中間窗口 `+8.830／-10.410`，最近窗口 `+0.737／+17.874`。雖然多數改變案例縮短持股週期且沒有本金不足，但早期所有 10 檔股票 ROI 都下降，中間窗口較強股群 5 檔也全部下降；其中早期金像電 `-17.523` 個百分點、富邦媒 `-9.701`、南電 `-9.670`，中間嘉澤 `-10.884`。最近窗口較強股群的大幅改善仍伴隨緯創 `-7.463`、南電 `-9.384`，無法補償跨股票與跨窗口的報酬代價。

DecisionDelta 為 0 前態不符、0 無效值、0 無成交排除，SQLite 完整。候選作用廣泛而非容量不足，但整體負向且以明顯 ROI 代價換取較短週期，依事前條件停止、不送 Sample B、不採用；候選程式、旗標、診斷掛勾與候選測試已移除，正式規則、Baseline 與 `T3/S39` 不變。本機證據保存在 [`RP-S01-A`](../exports/backtest-candidate-runs/rp-s01-a-price-seeking-peak-late-sell-plus1-t3s39-9y-fixed3y-600w-20260904/run-summary.md)及其 DecisionDelta。

## 後續實作順序

1. PP-T3-02（完成）：新增 SwiftData 欄位與穩定 enum，不在 UI 留下新舊雙重真相。
2. PP-T3-03（完成）：以價格路徑滾動 context 支援完整、局部、Yahoo 同日與 P10 試算。局部重算只在起點讀取最多 61 筆前態；若往前補入歷史行情，因價格路徑區段狀態具遞迴性，技術值仍由最早受影響日重播至最後一日，模擬範圍則維持原規劃。
3. PP-T3-04（完成）：List、Trade 與技術檢視直接讀取 `Trade` 持久狀態，不再於捲動或重啟後重算完整價格路徑；四種趨勢沿用相同箭頭，前期以淡色、後期以原色區分，實心買賣底色沿用不占欄寬的高對比襯底。
4. PP-T3-05（完成）：補齊單元與重算自動驗證，覆蓋 SwiftData 關閉重開、完整／局部／補歷史重播、Yahoo 同日、P10 隔離及舊版資料遷移。
5. PP-T3-06（完成）：以實際 store 完成 T3/S39 遷移驗證，確認每檔版本、重播完整性及人工操作保留結果。
6. PP-T3-06A（完成）：排除人工反轉明細存在時 `Stock.simReversed` 摘要可再度過期的路徑，補回歸測試；不改交易明細或策略。
7. PP-T3-07（完成）：完成 Sample A 零策略差異閘門；驗證用 T3/S39 同規則 DecisionBase、同版本控制重播，以及正式 T2/S39 Baseline 的 `periods.csv` 均一致。
8. PP-T3-07A（完成）：精簡緊湊摘要圖示；橫式二欄只精簡股群側欄，橫式三欄及約 10 吋直式 Page 並排開啟技術檢視時再精簡 Trade，一般寬度及技術檢視本身維持完整圖示。
9. PP-T3-08（完成）：App `v3.3.0 (55)（T3/S39）` 已發布，發布動作與後續資料池／Baseline 更新維持分離。
10. PP-T3-09（完成）：原 T2 資料池保留不覆寫；另建立 50 檔 T3/S39 資料池、A～E v3 固定輸入分片、十份 Baseline v21 與五份 DecisionBase v7，並完成跨 v20 位元零差異驗證。

任何階段若造成正式交易、Grade、ROI、平均持股週期或 `periods.csv` 差異，立即停止 T3 發布並追查；不得以「只是 UI」忽略差異。

## 2026/09/03 PP-T3-06 實際 store 驗證

- 10 吋確認機的正式 `default.store` 系統紀錄顯示，10 檔九年行情在 14:03～14:05 逐檔由 `T2/S39` 完整重播至 `T3/S39`。遷移後有行情的 10 檔版本全部正確，無 dirty marker；24,670 筆 Trade 中每檔前 40 筆維持未暖機，其後 raw value 均在 `1...9`，門檻、錨點、極值與未創新極值日數皆合法，SQLite `integrity_check` 為 `ok`。
- 為重現人工操作驗證，將 09:23 的遷移前實際 store 副本（`T2/S33`、10 檔、24,640 筆 Trade）放入暫用 iPad 第 9 代 Simulator。App 警告正確辨識 1 檔共 5 筆人工反轉；完成畫面回報 10 檔已套用 `T3/S39`、保留 5 筆、冗餘清除 0 筆、無法成立清除 0 筆。最終副本的 Trade 明細、Stock 摘要、版本、欄位與完整性全數一致；暫用 Simulator 已刪除，沒有替換正式確認機資料。
- 額外唯讀盤點發現，正式確認機的緯創仍有 5 筆 `Trade.simReversed` 明細，但 `Stock.simReversed` 為 false；相同不一致已存在於 T3 前的 S33 備份，而隔離遷移重播會正確修復摘要，因此不是 T3 欄位遺失。但摘要日後仍可再度過期，發布前須以 PP-T3-06A 找出操作路徑並補回歸測試。

## 2026/09/03 PP-T3-06A 人工操作摘要修復

- 根因是取消較早日期的反轉時，舊操作路徑只搜尋該日以前的反轉；若仍有較晚日期的反轉，`Trade.simReversed` 明細仍在，但 `Stock.simReversed` 會被錯誤留為 false。
- 反轉操作、完整／局部重播與 P10 還原現統一由 Trade 明細重建 `Stock.simReversed` 及 `Stock.simInvestUser`。App 啟動時另只查詢人工操作明細修復既有摘要快取，不執行 `tUpdate`／`simUpdate`；10 吋確認機實際啟動時修復 1 檔，對應先前發現的不一致。
- 回歸測試覆蓋「取消較早反轉但保留較晚反轉」及「App 啟動由持久 Trade 明細修復 Stock 摘要」；全套 119 項測試通過，另有 1 項需明確要求才執行的網路資料準備測試略過。這項修正不改 Trade 明細、策略判斷或持久欄位語意，維持 `T3/S39`。

## 2026/09/03 PP-T3-07 Sample A 零策略差異

- 先以 commit `89c769f5d20e8bf428cdfc68d302e361c68f2da9` 建立驗證用 T3/S39 同規則 DecisionBase `a-abcd9-v2-s32-an03-wow-nonbottom-no-ap02-add-penalty-20260901-t3-s39-89c769f5d20e-fixed3y-20260722-v6`。其 SQLite 完整性為 `ok`，包含 93,035 個決策事件、30 個期間結果、91 條規則、3 個窗口與 10 檔股票，`.complete` 與 `.p4b-complete` 皆精確對應 DecisionBase ID。
- 同規則 T3 基準與 `p3-z-baseline-control` 控制重播總分皆為 `115.6190465292565`；較強股群 `102.67438644282528`、較弱股群 `12.944660086431208`，ROI 與平均持股日數亦一致。DecisionDelta 的前態不符、首次分歧、事件變更、票數變更、閘門變更、結果差異與再收斂均為 0。
- 正式 Baseline v20（T2/S39）、驗證用 T3 基準及 T3 控制重播三份 `periods.csv` 位元一致，SHA-256 均為 `af80ae450872bb96c9a87fd319789eea3f60d2b2f4f32cf1ee0f036d902fcc11`。因此 T3 價格路徑持久化未改變 Sample A 的既有策略結果；這是 PP-T3-07 當時的發布閘門，後續已由 PP-T3-09 的 A～E 正式 v21 證據取代為現行基準。

## 2026/09/03 PP-T3-07A 緊湊橫式圖示精簡

- 未開啟技術檢視的直式 Page 維持原有摘要圖示。緊湊橫式二欄的股群側欄取消價格趨勢與 Grade 趨勢，只保留 Grade 等級，並縮減價格膠囊及圖示欄的實際占位；Trade 不變。
- 緊湊橫式三欄再取消 Trade 表頭與各列的價格趨勢、Grade 趨勢及 Grade 等級圖示，日期與成交價欄同步收窄；成交價欄仍保留膠囊左右間距，加碼欄靠左開始並擴大至足以完整顯示加碼後狀態，同時利用尾端空間。加碼狀態使用與成本價、報酬率相稱的小字級，單一「＋」維持原按鈕字級。技術檢視及一般寬度三欄維持原樣。
- 約 10 吋直式 Page 並排開啟技術檢視時，Trade 同樣取消上述三種摘要圖示並釋放占位，技術檢視本身維持完整圖示；版面政策的 4 項單元測試通過，並在 `simStock3 iPad 10.2-inch` 覆蓋安裝後逐一確認直式、橫式二欄與橫式三欄。

## 2026/09/03 PP-T3-08 App 發布

- App `v3.3.0 (55)（T3/S39）` 已由 commit `d1a5a81aa2736e1f90830bf7ecd25882e19ffb56` 推送至 `main`，並更新 GitHub `latest` 的 IPA 與 manifest。
- 發布測試共 58 項零失敗：`RecalculationTests` 47 項、`StrategyFitTests` 11 項。匯出的 IPA 已核對 Bundle ID、版本、build 與簽章。
- GitHub 遠端資產與本機 SHA-256 一致：IPA 為 `36e1729135f1af6f447e265fd89bd59fb91e4bf968b47bc8e6a6dd23d5a86b19`，manifest 為 `6eb4654cb8176b47a72a6e61a11b5abbc52767dfc454cae612951c607a3e7402`。
- 發布宣告為 `replay none`：發布動作本身未另跑 `tUpdate` 或 `simUpdate`。舊 T2 store 首次由新版 App 開啟時仍依 T3 遷移規則從最早受影響日重播，並重新驗證及保留仍有效的人工反轉與手動加碼。

## 2026/09/03 PP-T3-09 資料池與 Baseline 更新

- 原 `central-pool-t2s20-20160722-20260722-v1` 保留不覆寫；另由同一批 50 檔行情完整重播建立 `central-pool-t3s39-20160722-20260722-v1`。SQLite 完整性為 `ok`，50 檔均標示 `T3/S39`；121,447 筆 Trade 中 119,447 筆暖機後資料的階段、門檻、錨點、運行極值及未創新極值日數均合法，另 2,000 筆正好是每檔最初 40 筆未暖機資料。
- A～E 各建立 10 檔 v3 固定輸入分片，manifest 均為 T3 且 `simulationStateCopied=false`；正式 Baseline 由各固定窗口起點及全期間起點完整重播 S39，未把集中資料池的模擬結果當成 Baseline。
- Baseline v21 共十份正式報告，A～E 固定窗口分數依序為 `115.619／114.081／115.772／93.988／0.517`，全期間為 `111.477／103.727／113.048／82.239／0.797`。每份 manifest 都精確記錄 `T3/S39`、策略 S32 及 App 實作 commit `d1a5a81aa2736e1f90830bf7ecd25882e19ffb56`；完成標記、0 無效值及 0 無成交排除均已核對。
- 十份 v21 `periods.csv` 都和對應 v20 檔位元一致。五份 DecisionBase v7 各為 10 檔、3 窗口、30 筆期末結果與 91 條正式規則，`.complete`、`.p4b-complete`、P4b 與 SQLite 完整性全部完成。PP-T3-09 因此只更新持久資料與正式比較基準，不形成策略規則變更。
