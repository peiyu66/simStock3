# simStock3 工作交接：大盤現象投票零候選

> 交接時間：2026/09/03 07:31 +0800
>
> 來源交談：`查詢上個交談標題`
>
> 來源 task ID：`01a05f5c-93f4-7ca1-aac6-b8e42290143a`
>
> 交接前 HEAD：`db7fe6a13ccbc39e665f0b24db4e1d0a41612c92`
>
> 本文是當時快照；新交談預設讀取最新的 [`doc/工作交接.md`](../工作交接.md)。

## 正式狀態

- Branch `main`；依未更新的本機遠端追蹤參照，比 `origin/main` ahead 4。工作樹不乾淨，大盤 I01／Q0／D1 尚未 commit 或 push。
- App `v3.2.8 Build 54`；Baseline v20；策略 S32；資料規則 `T2/S39`。
- 正式規則 commit：`48d42e50ada5dcc0ed2e22dffff6871323daf8b7`。
- A～E 固定三年分數：`115.619／114.081／115.772／93.988／0.517`；全期間：`111.477／103.727／113.048／82.239／0.797`。
- 10 份正式報告 manifest 與五份 DecisionBase 已重新核對同一資料／規則身分；DecisionBase 各 3 窗口、10 檔、91 條規則，完成標記、P4b 與 SQLite 完整性均通過。
- 本階段沒有正式採用市場票，也沒有修改 App schema、Baseline 或 T/S。

## 已完成且已提交

- `b8f0853` 建立交接制度與未來價格路徑研究計畫。
- `ad97cb3` 保存未來路徑研究工具。全域 EBM 沒有穩定新組合；正式條件可手動重現 `L-P03／L-P04`，但 `RB01-A01` 只辨識 G1 `2/15`，未達 `10/15`，故停止未知規則搜尋。這是方法資格失敗，不是否定正式規則。
- `03ee992` 記錄 `A-E01-D20-S1`：A `+0.889`、B `-5.430`；依事前停止條件不採用、不跑 C／D 或全期間，正式維持 38 日。
- `db7fe6a` 完成可重用非正式 TAIEX 研究資料池：127 個月份、2,569 個市場日、101 個 MT1 欄位、不可變前綴及 15 個 Sample×窗口分片；真實 TAIEX 成為後續共同市場控制。研究資料池不是 App 正式資料庫。

## 已完成但未提交

- `MKT-R02-I01`：1,036 個現象原子；943 個 canonical 通過三窗口容量，其中 467 個單一價格現象可直接研究。約 17.5 萬個二現象只表示空間過大，不授權全量回測。權威產物為 `mkt-r02-i01-market-votes-20260902-23c3fc529e8d`。
- `MKT-R02-Q0`：停用與永假市場接線控制都與 Baseline 零差異；同一診斷現象分別注入 H買／L買／賣出／加碼時，市場票、phase 與直接動作差異均可觀察。這只證明 `DEBUG` 接線，不是績效結論。31 項大盤離線測試及 Swift 103 項測試通過（0 失敗、1 略過）。
- `MKT-R02-D1`：v20 A～D 共 3,024 輪新持股，978 輪含關鍵票。W1 的 631 組容量合格交互留下 4 列，實質是 `H-P01 × 60 日均線狀態` 的互補表示；兩列在 W2 容量不足，另兩列效果由 `+0.683` 反轉為 `-0.115`、`p=0.250`。W2 確認 0，不開 W3、不建立 C1。合成植入控制通過；正式產物為 `mkt-r02-d1-entry-interaction-d4002fa43b79`。
- D1 初版無 checkpoint 已中斷並修正效能與進度輸出；後續補正真實／虛無容量對稱及產物自解釋欄位。只有 methodVersion 4 保留 `.complete`，三個舊產物均標為無效或被取代。

## 授權與工作樹邊界

- 無已核准的候選、診斷、Baseline、commit 或 push 任務。
- 未提交差異包含 `AGENTS.md`、文件索引與兩份大盤計畫、candidate runner、兩個既有 Swift 檔、新增 `InternalMarketVoteResearch.swift`、I01／Q0／D1 三工具及三測試，以及本次交接文件。
- Q0 接線預設關閉且僅限 `DEBUG` 候選旗標；正式 App 行為、版本及 T/S 均未改。研究產物在 Git 排除的 `exports/`，不會隨一般 commit 自動納入。
- 建立這份交接不代表 commit 或 push；使用者要求交接前，僅曾收到「可整理 commit」的建議，沒有授權執行。

## 僅討論、尚未授權

- 是否將 I01／Q0／D1 的程式、計畫、零結果及交接整理成一個本機 commit；不包含 push。
- 是否另擬賣出或加碼階段的大盤交互實驗。D1 只否定目前 H買／L買進場與 467 個單價格現象的可重播候選，不能證明其他 phase 無關；新實驗須另行設計與核准。

## 已停止方向

- 不開未來路徑 `RB01-D01`、保留集、三數值或事後門檻補救。
- 不採用 A-E01 20 日冷卻，不再跑其他樣本或日數。
- 撤回大盤 Q1 的 15 條 G1 逐一移除／復原，不再反覆用已知規則回收作未知大盤研究的前置任務。
- D1 不以二現象、成交量確認或放寬門檻補救；沒有候選，因此 C1 不啟動。
- 非正式資料池與 Q0 接線不冒充 App 正式市場資料；只有正式採用後才另案設計 schema、下載、重算與遷移。
- 不重開 App Store 上架研究。
