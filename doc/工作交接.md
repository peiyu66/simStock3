# simStock3 工作交接

> 最後更新：2026/09/03 07:54 +0800
>
> 歷史快照：[2026/09/03 大盤現象投票零候選](工作交接歷史/2026-09-03-0731-大盤現象投票零候選.md)
>
> 本文是新交談的閱讀路由與當時快照，不取代 Git、manifest、[現行回測規則](現行回測規則.md)或[回測規則驗證](回測規則驗證.md)。

## 交接身分

- 來源交談：`查詢上個交談標題`
- 來源 task ID：`01a05f5c-93f4-7ca1-aac6-b8e42290143a`
- Branch：`main`
- 交接前 HEAD：`db7fe6a13ccbc39e665f0b24db4e1d0a41612c92`
- 本機記錄的 `origin/main`：`e5fe6370f5f5d6f1e53653543be285cf09355af3`；本次未連線 fetch。大盤現象 I01、Q0、D1 的程式、計畫、接線與交接紀錄由包含本文的同一個本機 commit 記錄，未 push。

## 正式現況

- App：`v3.2.8 Build 54`
- Baseline：v20
- 策略版本：S32
- 資料規則：`T2/S39`
- 正式規則 commit：`48d42e50ada5dcc0ed2e22dffff6871323daf8b7`
- A／B／C／D／E 的固定三年與全期間共 10 份 manifest 已於本次交接重新核對，Sample、`T2/S39`、S32、規則 commit、截止日 `2026/07/22`、600 萬元及兩次加碼一致，完成標記均符合 run ID。
- 五份 DecisionBase v6 各為 3 窗口、10 檔股票、91 條規則，`.complete`、`.p4b-complete` 與 SQLite `integrity_check=ok`。
- App Store 上架已結案；現行維持 GitHub `latest` 與已登記裝置的 Ad Hoc 安裝。

| 樣本 | 固定三年 | 全期間 |
|---|---:|---:|
| A | 115.619 | 111.477 |
| B | 114.081 | 103.727 |
| C | 115.772 | 113.048 |
| D | 93.988 | 82.239 |
| E | 0.517 | 0.797 |

A／B／C／D 是均衡樣本；E 是全弱勢壓力樣本，其絕對分數不與前四組排名。精確股群、規則與報告入口見[現行 Baseline](現行回測規則.md#現行-baselineabcde-v20)。本交談的研究沒有改變上述正式狀態。

## 本階段已完成且已 commit

包含本文的新提交之前，已有下列 4 個本機 commit 尚未 push：

1. `b8f0853`：建立工作交接制度及未來價格路徑研究計畫。
2. `ad97cb3`：完成未來 20 日價格路徑研究工具與正向控制補救。全域 EBM 沒有穩定新組合；手動正式條件可重現 `L-P03／L-P04`，但 `RB01-A01` 只辨識 G1 `2/15`，未達 `10/15`，依停止條件不執行通用語法未知搜尋 `RB01-D01`。這否定的是該路徑標籤／方法的發現資格，不是否定正式規則或技術數值。
3. `03ee992`：`A-E01-D20-S1` 將一般加碼冷卻 38 日改為 20 日；Sample A `+0.889`，Sample B `-5.430` 且兩股群、ROI、週期、輪次都退步。依事前停止條件不採用、不跑 C／D 或全期間，正式維持 38 日。
4. `db7fe6a`：建立可重用非正式 TAIEX 研究資料池。`MKT-D01-P0～P3` 與 `R1` 完成：127 個月兩來源原始資料、2,569 日正規化資料、101 個 `MT1` 技術／活動欄位、不可變前綴及 A～E 共 15 個窗口分片均通過；後續研究以真實 TAIEX 作共同市場控制。研究資料池不是 App 正式資料庫。

## 本階段提交範圍

下列成果由包含本文的同一個本機 commit 記錄；詳細方法、產物與停止條件見[大盤現象投票研究計畫](大盤現象投票研究計畫.md)。三段均未改正式 schema、Baseline 或 `T2/S39`：

1. `MKT-R02-I01`：盤點 101 個 MT1 欄位，產生 1,036 個現象原子；1,002 個 canonical 中有 943 個通過三窗口容量，包含 467 個可單獨投票的價格現象與 476 個只作第二條確認的市場活動現象。約 17.5 萬個二現象只證明候選空間過大，禁止直接全量回測。現行權威產物為 `mkt-r02-i01-market-votes-20260902-23c3fc529e8d`；較早的 `9d20f54bc939` 仍保留完成標記，但 D1 與計畫都明確使用前者。
2. `MKT-R02-Q0`：建立預設關閉、僅限 `DEBUG` 候選旗標啟動的市場票接線。Baseline 停用與載入 MT1 但永假的控制均為逐事件、逐票、動作、結果及 `periods.csv` 零差異；診斷脈衝分別在 H買／L買／賣出／加碼產生正確票與直接動作差異，證明接線可觀察，不能解讀為現象有效。31 項大盤離線測試與完整 Swift 103 項測試通過（0 失敗、1 項依設計略過）。權威產物為 `mkt-r02-q0-market-vote-socket-20260902-a843bd5326ee`。
3. `MKT-R02-D1`：直接檢驗「關鍵 H／L 進場票 × 單一大盤價格現象」與完整持有輪效率的交互。v20 A～D 有 3,024 輪新持股、978 輪含關鍵票，展開 2,118 筆、21 條規則；W1 的 631 組容量合格交互留下 4 列，實質都為 `H-P01 × 60 日均線狀態` 的互補表示。兩列到 W2 容量不足；另兩列的標準化效果由 `+0.683` 反轉為 `-0.115`，`p=0.250`，W2 確認為 0，故不開 W3、不建立 `MKT-R02-C1`。人工植入交互可被正確找回，方法控制通過。唯一保有 `.complete` 的正式產物為 `mkt-r02-d1-entry-interaction-d4002fa43b79`；三份較早 D1 診斷產物均已移除 `.complete` 並寫明無效或取代原因。
4. D1 無進度異常已依約定連續診斷：初版在虛無迴圈重算固定摘要，超過 60 秒無 checkpoint 時中斷；改為預算固定量並每 50 次回報。其後補正真實／虛無相同容量門檻、空表表頭、已結案輪敏感度及 W2 失敗原因；研究門檻未因結果調整。最後 13 項 I01／Q0／D1 Python 測試、產物完整性及 `git diff --check` 通過。

## 已授權但尚未完成

- 無已核准的候選、診斷、Baseline 或 push 任務。

## 僅討論、尚未授權

- 是否另擬賣出或加碼階段的「大盤現象 × 現行策略規則」直接交互實驗。D1 的零結果只涵蓋 H買／L買新進場與 467 個單一價格現象，不能外推為大盤對賣出／加碼無關；但新實驗必須先精確定義當日關鍵票、結果單位、共同市場日控制及停止條件，再另行核准。

## 已停止方向

- 未來價格路徑的 EBM、單格掃描及 `RB01` 回收基準都沒有取得未知規則發現資格；不開保留集、不執行 `RB01-D01`，也不改標籤、放寬門檻或加三數值補救。
- `A-E01` 20 日冷卻不採用；不跑 C／D、全期間或其他日數。
- 大盤研究的「逐一移除／復原 15 條 G1」Q1 已撤回，不再把反覆找回既有規則當成未知大盤研究的前置任務。
- D1 沒有形成候選，因此不啟動 `MKT-R02-C1`；不以二現象、成交量確認或放寬 W1／W2 門檻救結果。
- 非正式大盤資料池與 Q0 `DEBUG` 接線不等於 App 正式市場資料。只有市場規則通過完整回測並正式採用，才另案設計 App schema、下載、重算與遷移。
- 不重開 App Store 上架可行性研究。

## 工作樹與接手注意事項

- 本次 commit 包含 `AGENTS.md`、文件索引、前置與投票研究計畫、Q0 `DEBUG` 接線、runner、I01／Q0／D1 工具與測試，以及本交接與歷史快照；這些都是同一大盤研究範圍。
- `exports/market-data/taiex/`、Q0 candidate runs／DecisionDelta 與 D1 統計產物受 Git 排除，只是本機證據；正式結果要依本文件列出的 run ID、manifest 與 `.complete` 核對。
- Q0 接線只在 `DEBUG` 及明確候選旗標下生效，預設不啟用；尚未正式採用任何市場票。若只想保留研究工具而移除 App 接線，這會改變已完成 Q0 的可重現能力，必須先與使用者定案，不能自行清除。

## 新交談起始方式

1. 完整讀取本文。
2. 核對 `git status --short --branch`、`git rev-parse HEAD`、正式 v20 manifest、五份 DecisionBase，以及 D1 `mkt-r02-d1-entry-interaction-d4002fa43b79` 的 manifest／品質報告／`.complete`。
3. 先向使用者回報：正式策略仍為 Baseline v20／S32／`T2/S39`；大盤資料與研究方法已建成，但 D1 在 W2 反轉而以零候選結案；不是自動續跑 C1。
4. 未取得具體核准前，不執行新研究、不修改正式規則、不 push。交接列出的下一方向不是自動授權。
