# simStock3 工作約定

## 約定維護

- `AGENTS.md` 是以 Codex 為主要對象的專案工作筆記。交談中形成明確、穩定且跨任務仍適用的新約定時，Codex 應主動補充或修正本檔，不必等待使用者特別提醒。
- 不機密但不宜上傳的內部開發規範、風險判斷與專案雜項，統一放在本機 `internal-docs/`；該目錄整體不納入 Git。開始相關任務時，若目錄存在，先讀取其中的 `README.md` 與相關文件；公開文件不得直接連到該目錄內的檔案。
- 每個新的 simStock3 交談開始時，必須先完整讀取 `doc/工作交接.md`，再以 Git、manifest、`doc/現行回測規則.md` 與 `doc/回測規則驗證.md` 核對易變狀態；交接只是當時快照與閱讀路由，不凌駕權威文件，也不因其列出下一項就自動取得執行授權。
- 使用者說「請交接」或同義指令時，先重建目前正式狀態，再更新 `doc/工作交接.md` 並新增一份 `doc/工作交接歷史/YYYY-MM-DD-HHmm-主題.md`；`doc/工作交接.md` 的交接身分固定記錄來源 Codex 交談標題與 task ID，另以 Git HEAD 定位程式碼狀態。歷史快照建立後不回寫，後續修正以新交接說明。內容必須區分已完成、已授權未完成、僅討論未授權、已停止方向及工作樹狀態；建立交接不自動代表 commit 或 push。
- 完成目前已核准任務時，Codex 應主動提示任務已完成，列出下一個排隊任務、範圍及主要影響，並先徵詢使用者許可；不得因已有任務清單就自動接續執行。使用者對已明確列出的下一項回覆「好」或同義肯定時，才依既有約定視為授權。
- 新約定提出時，先檢查是否與本檔或其他權威文件重疊、涵蓋範圍不同或互相矛盾；若有矛盾，應明確指出兩種做法及實際影響，待使用者定案後合併或移除被取代的說法，不讓衝突約定並存。
- 工作判讀若出現與 App 開發理念、實際使用情境、既定證據權重或使用者觀點互相矛盾的看法，不應自行選一邊或隱藏反證；先說明矛盾、各自成立的前提與對決策的影響，再與使用者共同定案。
- 開始前若預估已核准的單一任務無法一次完成或必須拆成更多小任務，先說明新增段落、資源成本及能解鎖的決策，待確認後才擴張；不得自行延伸成新的前置專案。撤銷「約 30 分鐘內可持續等待」的容許基準：只要耗時顯得可疑、沒有可觀察進度或可靠新產物，就立即中斷並保留現場診斷，不以程序仍存在、尚未逾時或可能再等就完成作為盲等理由。修復當次任務後，還須修正 runner／完成標記／失敗偵測，或記錄根因、可疑訊號與恢復方式，防止相同問題重現。
- 謹慎管理 Codex 額度與運算資源：優先重用既有 DecisionBase、報告、資料快照與已驗證工具，先採足以解鎖當前決策的最低成本檢查；不為形式完整而新增不影響決策的文件、候選地圖、全量下載、重複建置或完整回測。必要驗證不得因節省額度而省略，但執行前應先確認它直接服務目前核准的任務。
- 已核准任務內可預期的本機路徑銜接、快照暫存、格式轉換與安全重試由 Codex 自主完成，不因一般耗時或可局部修正的工程問題反覆請示；只有發現會顯著擴張範圍／額度、改變已確認設計、可能破壞既有資料，或前期規劃未揭露的重大風險時才暫停討論。長時間工作必須能提出持續更新的進度證據、分段成果或可讀失敗紀錄；一旦進度證據停止或與預期階段不符，就主動中斷診斷，不以固定短逾時機械判斷，也不因尚未到長逾時上限而繼續盲等。
- 本檔只保留需要跨任務提醒、專案特有且無法由程式或權威文件自然保證的約束；不保留 Codex 本來就會執行的一般做法，也不寫入單次指示、暫時候選、當期分數或容易隨 Baseline 改變的資料。後者應留在對應報告、驗證文件或交談摘要。
- 下次整理文件或準備文件 commit 時，主動檢查本檔是否有重複、互相衝突、單條過長或只適用特定工作流程的內容；需要時將細節分流到專用流程文件，由本檔保留核心約束與明確的任務閱讀路由，並向使用者回報整理結果。
- 建立或更新文件時，主動辨識值得解釋的專案自訂名詞與跨文件概念，在每個段落第一次有助理解的出現處連到權威說明文件或標題錨點；尤其「實年報酬率／真年報酬率」等自訂名詞不得只靠字面猜測。避免同一段反覆加連結；定義變更時同步檢查主要入口與交叉連結。
- Markdown 表格的第一欄若主要放規則、實驗或候選代號，表頭應明寫為「規則代號／實驗代號／候選代號」，利用至少四個中文字的自然寬度保留約六個英數字元，優先避免代號在 13 吋 iPad 橫式 Safari 中折行；說明欄維持彈性換行，不為固定欄寬改寫成難維護的大段 HTML。
- 回測判讀或使用者要求擇一時，第一句直接給出結論；預設只補最關鍵理由、主要顧慮與下一步，不重述完整結果，除非使用者要求展開。
- 與使用者對話採「適度簡化」而非硬性字數限制：先給結論，保留理解決策所需的原因、風險與下一步，避免一次展開過多技術細節；重要概念可適度說明，程式與研究細節待使用者追問再展開。
- 主動修改本檔後應告知使用者；是否 commit 仍遵循本專案既定 Git 約定。

## 專案原則

- simStock3 是台灣上市股票的個人技術模擬與研究工具，不是券商下單工具，也不以新聞、基本面或主觀預測決定買賣；盤中資訊只用於更新當日模擬與協助掛單，不宣稱能預測收盤或未來績效。
- 本專案已決定不再以 App Store 上架為目標，現行維持 GitHub `latest` 與已登記裝置的 Ad Hoc 安裝。App Store 上架可行性不是未結案風險或待辦；只有使用者日後明確改變決定時，才重開規範、功能與行情授權評估。
- 核心目標是以簡單、一致、可長期回測的規則，在報酬與持股週期間取得平衡；優先追求保本、小賺、較短週期及現金流，不為少數歷史暴利或單一最高分犧牲穩定性。
- App 的主要使用情境是對持股反覆進行較短週期的買賣，不是假設從同一起始狀態對同一股票連續運作九年。因此規則採納以多個不重疊的固定三年窗口為主要證據：它讓規則在不同市場階段有多次被推翻的機會，也更接近實際用途。九年全期間只用來觀察單一起始路徑長期累積後的路徑依賴與風險，應如實記錄但不自動否決固定窗口有效的規則；只有全期間反證同時揭露資料錯誤、資金失控、跨樣本普遍重大退步或其他會危及實際用途的問題時，才提高其決策權重並暫停討論。
- 回測是比較規則的工具，不是未來績效保證。評估時應重視不同股票類型、不同三年窗口與不利情境能否維持。單股效果只用來追查錯誤、集中度或形成待驗假設，不再以單一股票設計或採用規則；候選必須經固定完整樣本重播，只有擴大驗證後仍具可解釋效果且沒有不可接受代價，才可考慮採用。
- 資金紀律是策略的一部分：首次買進只使用個股預算的一部分，預留資金供預設兩次加碼；分散於股群、持續依相同規則累積多輪結果，不能把單次交易或單一股票的輸贏當成策略成敗。
- 選股評等與紅星來自歷史模擬的實年報酬率及平均持股週期，只作為熱門程度與選股參考。核心買賣規則由開發者驗證後統一維護，不把臨時研究條件任意暴露為一般使用者設定。
- 模擬以低買高賣價差為主，忽略股息、股利、增減資等影響；所有報告與說明都應清楚維持這項界限。
- 股票技術數值與逐日模擬是 App 的主要效率瓶頸。新增需要固定回看窗口或跨日累積的規則時，應把 `SimulationRollingContext` 類型的滾動狀態列為效能改善方案：完整重播從已知初始值開始，部分重算只在起點讀取必要歷史一次，後續依日期順序以當日事件更新，不讓每筆 Trade 重複查詢同一段歷史。Context 只保存規則所需的摘要並限於單一股票／窗口／情境；P10 各假設價格及 Yahoo 同日試算各自複製正式前態，不能互相污染。是否持久化仍依隨機查詢、歷史顯示與重建成本個別決定，不因新增構想就直接擴充 schema 或通用資料欄位。Context 不是可任意堆放構想的通用欄位集合；每個正式變數都必須由具體任務提出、完成單一變因檢驗並獲得採用，候選期間可臨時加入，但未採用時須與候選程式一併移除。用來解釋機制的 60 日搜尋等例子，不視為已核准的效能改造任務。

## Git 與發布

- 使用者要求「commit」時：先整理本次範圍、檢查差異並完成相稱的驗證，再建立 commit。功能完成且驗證通過、準備換主題或修改已累積過多時，主動提醒適合 commit。
- 使用者要求「push」時：先檢查遠端與本機狀態，整理、驗證、commit、push。只要變更會影響 App 程式、建置或安裝內容，即視為完整發布流程，還要更新 GitHub `latest` release 的 IPA 與 manifest，並核對遠端發布檔。若差異只有文件、工作約定或其他不影響發布產物的內容，Codex 應主動判斷為 Git-only push，略過 App 重建與 `latest` 更新，不必由使用者逐次提醒；若範圍有疑義，應在執行前明確說明判斷。
- Push 與完整發布優先使用 `scripts/push-release.sh`，操作與失敗恢復以 `doc/發布流程.md` 為準。腳本必須一開始就在可存取 GitHub 網路、macOS Keychain、Xcode signing 與 Simulator 的核准環境執行；不得因 sandbox 內 `gh auth status` 的 invalid、`security find-identity` 的 0 identities 或首次 `codesign` 信任查詢失敗，就要求使用者重新認證或更換憑證。只有相同檢查在正式發布環境仍失敗時才請使用者介入。
- 每次 push 前主動依本次累積變更評估 App 版本應維持、推進 patch、minor 或 major，並在發布前或完成回報中提出明確建議。尚未獲使用者採納或拒絕的升版建議，後續每次 push 都要繼續提示；採納即按建議升版，拒絕後不再為相同變更範圍重複建議，除非又有足以改變判斷的實質變更。
- `README` 不維護會隨每次發布過期的目前 App 版本／build；安裝入口固定指向 GitHub `latest`，實際版本由 App、Xcode 專案及 release manifest 提供。一般文件需要說明格式時使用 `vX.Y.Z (Build)（Tn/Sn）` 等明確示例；若發布記錄或回測重現必須寫具體版本，應同時標示 App 版本／build 與 `Tn/Sn`，或明確說明只記錄其中一種版本的理由。只解釋資料遷移的段落可僅標相關 `Tn/Sn`。
- 使用者說「採用為 Baseline」或「建立 Baseline」時，才觸發正式 Baseline 流程；一般 commit／push 不自行把候選視為已採用。正式流程先提交已驗證的規則程式，再以該規則 commit 建立固定窗口與全期間 Baseline、更新報告及驗證文件，必要時另建文件 commit 記錄精確規則 commit，最後才發布。執行前與完成後都須用 Git 驗證 manifest 記錄的完整 commit 確實存在且精確相等，不以七碼前綴相同視為有效。每份 Baseline 的 `baseline.json`、`manifest.json`、HTML 報告與驗證文件都必須分別記錄 `Tn/Sn` 資料規則版本及策略規則版本，不以單一「規則版本」含糊合併。使用者不必分別下達兩次 commit 指令。
- 正式 Baseline 必須讓當期所有正式股票樣本都以相同規則 commit、`Tn/Sn`、策略版本、輸入截止日、本金與加碼設定，分別完成固定窗口及全期間報告；不得再以固定窗口證據已足夠或避免形式性重播為由省略部分樣本的全期間。全期間仍只作九年單一路徑的壓力證據，不推翻固定窗口的採用權重，但必須用來確認資料錯誤、資金失控、跨樣本重大退步或其他意外；發現重大意外時依既有證據權重規則暫停定案。`Baseline歷史.md` 另以 A～E 並列表保存各樣本固定／全期間分數與報告連結，並記錄策略、T/S、規則 commit 與 DecisionBase 版本關聯。
- 遠端已有新提交時，先 fetch 並比較差異，再安全整合；不得直接覆蓋本機或遠端修改。
- 回測資料庫、匯出報告、IPA、建置暫存與 Xcode 個人狀態不納入一般程式 commit；只有長期有效的規則結論與文件進入 Git。
- `Tn/Sn` 資料規則版本與 App 版本／build 分開管理。`simUpdate` 的規則、持久結果或狀態語意改變時，推進 `currentSimulationStateVersion`（S）並完整重跑模擬；`tUpdate` 的技術值、持久結果或狀態語意改變時，推進 `currentTechnicalStateVersion`（T），完整重跑技術值及其下游模擬；兩者都改變才同時推進 T/S。若修改只重構實作或回溯取得方式，且已由原公式逐值對照、相關重算測試及正式 Baseline 控制重播證明持久結果與決策零差異，維持原 T/S 且不重建 Baseline；證據不足或出現任何差異時，停止發布，先補足驗證或依資料語意變更處理，不得自行視為純重構。單純 App build 變更不觸發全量重算。資料遷移須保留有效期間內的手動反轉與加碼，不得改用會清除使用者操作的 `simResetAll`。
- `S22` 是 SwiftData schema 遷移，不只是 `simUpdate` 版號提升：`Trade` 新增持久欄位 `simFitTrendPhaseRaw`。任何正式 App、集中資料池、Baseline、`browse.store`、`period-*.store`、文件種子、測試 fixture、匯入／還原備份或其他舊 `.store`，都必須先依 `doc/Grade趨勢預警狀態持久化計畫.md` 判斷其用途與版本；能啟動 App、能開啟 store 或欄位採預設值 `0` 都不代表已完成 S22。只有確認 schema 欄位存在、目標 Stock 全部標為 S22、完整重播結束且人工操作已重驗，才可稱為已遷移；遷移中斷造成新舊版本並存時必須續跑，不得把未完成 store 用作 Baseline、DecisionBase 或 S21／S22 零差異對照。舊 App 不得回開已遷移的 S22 store。
- `S31` 再新增 `Trade.simFitTrendPhaseExtreme`，並把改善／惡化確認拆成探頂／拉回及探底／反彈。舊 S30 store 即使能自動增加可選欄位，仍須從最早受影響日完整重播、逐檔升至 S31 並重新驗證人工反轉與加碼；欄位全空不得視為遷移完成。DecisionBase 因新增 `fit_trend_phase_extreme` 推進為 v6，manifest、SQLite metadata 與目錄尾碼必須一致。正式 Baseline 切換前，既有規則只能以兩個新階段的聯集維持原確認範圍，不得讓資料遷移本身改變買賣條件。
- `S32／S33` 不增加 schema 欄位：S32 在惡化反彈每日下降超過 `0.3` 時重設探底，S33 將改善／惡化共同起轉門檻由 `0.1` 提高為 `0.3`。兩版都屬 `simUpdate` 狀態語意變更，正式 App、Baseline 與 DecisionBase 必須從最早受影響日完整重播；T2 不重算，既有規則仍以兩個確認分段的聯集維持原票數範圍。
- `T3` 在 `Trade` 新增價格路徑階段、凍結門檻、錨點、運行極值及未創新極值日數五個持久欄位。舊 T2 store 即使能自動開啟並取得預設值，仍須從最早受影響日完整重播 `tUpdate` 及下游 `simUpdate`，逐檔升至 T3；欄位定義與遷移範圍以 `doc/價格路徑狀態持久化計畫.md` 為準，舊 App 不得回開已遷移的 T3 store。
- 正式 App 的全期間重算與局部重算統一視為「從最早受影響日往後重播」；不得先整批刪除後續人工操作。每筆反轉買賣與手動加碼都須以未套用該筆人工操作的正常結果重新驗證：仍改變結果者保留，正常結果已相同者清除為冗餘，因持股或交易狀態不再成立者清除為無效，最後由存活操作重建 Stock 摘要。P10 各檔價格試算必須各自套用同一份正式人工意圖且互不污染，也不得以假設價格清除正式記號。T/S 遷移是所有正式行情、P10、完整／局部重算共用的前置閘門；未完成時不得先更新最後一日或試算，其他作業忙碌時必須排隊而不得遺失遷移要求。App 強制升級 T/S 且存在人工操作時，先顯示不可取消的重算警告；完成後不論有無人工操作都回報遷移股票數，有人工操作時另列保留、冗餘清除與無效清除筆數。
- 每次 push 並更新 GitHub `latest` 後，完成回報必須列出 App 版本／build、`Tn/Sn`、是否觸發 `tUpdate`／`simUpdate` 重算，以及是否保留手動反轉與手動加碼；若不觸發資料重算，也要明確寫出。

## Simulator 與 UI 驗證

- 使用者文件的標準介面截圖與全新展示資料庫依 `doc/使用者文件截圖流程.md` 執行；展示股群不屬於回測樣本或選股推薦。截圖資料優先由集中資料池複製行情與現行 `T`，再由新 App 重播現行 `S`，不為文件重複下載完整歷史行情。
- 主要設計與一般功能驗證預設使用 `iPad Pro 13-inch (M5)`；13 吋橫式三欄版面是優先保護的 UI 基準，README 截圖也使用此裝置。
- 約 10 吋裝置使用 `simStock3 iPad 10.2-inch`（iPad 第 9 代），驗證緊湊橫式、直式版面及執行單元測試。每次 UI 版面調整完成後，都必須將該次已建置的 App 覆蓋安裝到 10 吋 Simulator、開啟並保持裝置開機，讓使用者直接確認；13 吋仍是主要設計基準及其他後續測試的預設裝置，不得因 10 吋確認而忽略或改壞。
- 小尺寸與多工版面應依 App 實際可用寬度切換，不為各 iPad 型號各寫一套，也不得為改善 10 吋而改壞 13 吋基準。
- List、橫式側欄與 Page 的共同股票摘要應沿用相同欄位順序與圖示位置；漲跌停符號固定放在股價欄內，歷史補齊狀態固定接在股價之後。除下列緊湊橫式省略規則外，條件式圖示未出現時仍須保留等寬占位，避免股票名稱、股價、Grade 或後續欄位因單列狀態不同而位移。
- 直式與一般寬度版面的 20 日價格趨勢圖示固定放在 List／橫式側欄與 Trade 的成交價膠囊右側，和漲跌停符號同屬價格欄；不得為此增加外部欄寬，並須保留極小等寬位置避免各列抖動。承低、追高或賣出的實心底色上須替趨勢圖示加不占欄寬的高對比襯底；沒有漲跌停時不在膠囊內保留空白漲跌停槽，緊湊橫式側欄另縮小漲跌停符號及其占位，優先讓四位數股價完整顯示。
- 緊湊橫式二欄的股群側欄取消價格趨勢與 Grade 趨勢圖示，只保留 Grade 等級圖示，並實際收回圖示占位供其他欄位使用；Trade 維持完整圖示。緊湊橫式三欄再取消 Trade 的價格趨勢、Grade 趨勢及 Grade 等級圖示與占位，技術檢視維持完整圖示；一般寬度三欄及所有直式版面不套用此省略規則。
- 價格趨勢的探頂、拉回、探底、反彈沿用相同方向圖示，以前期淡色、後期原色區分成熟度，不另加上下短線或改變箭頭方向；九階段 raw value、門檻與持久化邊界以 `doc/價格路徑狀態持久化計畫.md` 為準。
- 緊湊橫式 Trade 列欄寬不足時，優先依上述三欄規則省略摘要圖示，再縮減日期、價格、成本價、報酬率的欄寬與字級，以及欄間留白，把空間保留給買賣狀態、數量及持股天數完整顯示；省略圖示後仍須保留成交價膠囊左右的必要間距。三欄加碼欄須靠左開始並延伸使用尾端空間，使單一「＋」位置不會過度靠右，且「已加碼」等狀態文字可完整顯示；一般寬度版面維持原字級與比例。
- 「建置驗證」只代表可以編譯；需要實際操作確認時，必須把該次已驗證的 `.app` 覆蓋安裝到指定 Simulator。不得先刪除 App 或清除資料。
- `xcodebuild test` 與 `xcrun simctl boot／bootstatus／install／launch` 等 Simulator 操作應一開始就使用核准的 Simulator 執行權限，不先在受限環境試跑。執行測試前先檢查指定裝置；若尚未開機，先 `boot` 並等待 `bootstatus` 完成，再啟動 `xcodebuild`。
- `@MainActor` XCTest 若在方法內直接建立或持有 SwiftData `@Model`、`Technical` 或其他 MainActor 隔離 fixture，測試入口須宣告為 `async`，讓 fixture 在相同 executor 完成生命週期；同步入口即使斷言全數完成，也可能在 Swift concurrency `TaskLocal` teardown 跨 executor 解構而觸發 malloc double free。遇到此型崩潰時先以只改 `async` 的單一變因重播確認，不得改斷言或正式程式掩蓋。
- 使用 `simctl launch` 以不同命令列參數切換內部回測候選或 Sample 前，先終止仍在執行的 simStock3；否則 Simulator 可能沿用舊 PID，不套用新的啟動參數，造成看似長時間執行但實際未開始新任務。
- Xcode 測試可能建立 Simulator Clone 並暫時關閉基準裝置；若結果已明確顯示 `TEST SUCCEEDED`，其後單獨出現的已知 Clone launch／GUI crash 訊息不視為測試失敗，也不在無關任務中追查。若測試仍在等待且基準裝置已關機，可重新啟動基準裝置讓 Clone 流程完成。
- 若驗證過程啟動了 Simulator，完成後保持該裝置開機；UI 或回測瀏覽驗證時，儘量讓 simStock3 停留在相關畫面，方便使用者直接檢查且不會混淆版本。
- 安裝或測試可能改動現有 Baseline／測試資料時，先說明影響，並優先使用資料庫副本或 Simulator Clone，不直接破壞固定測試環境。
- 回測瀏覽器使用正式 Page 的最新 UI，不維護另一套舊 UI；報告則保留產生模擬數值時的規則版本，UI 更新不得冒充新回測結果。
- 模擬起始日前的 Trade 只作為技術數值準備期；是否屬於準備期一律以交易日和 `stock.dateStart` 比較為準。`simRule == "_"` 是需同步保存的顯示標記，不是唯一判斷來源；新增或往前補入準備期資料時應立即寫入，畫面一律灰色且不得提供反轉買賣或加碼操作。

## TWSE 歷史資料下載

- TWSE 個股或大盤月份歷史資料的批次下載、節流、退避、續跑與發布閘門，以 `doc/TWSE歷史資料下載標準.md` 的公開安全原則為權威。所有請求維持單線及程式既定的保守間隔，不得為縮短作業時間提高頻率；一旦出現逾時、非 JSON、服務限制或其他伺服器異常，立即降速並依有限退避停止或續跑，不探測、逼近或規避服務限制，也不把專案工程值誤稱為 TWSE 官方容許量。精確操作參數只保存在程式與本機內部筆記，不在公開工作約定重複。

## 買賣規則回測

- 一般固定三年候選的建置、Simulator 啟動、完成標記等候、輸出搬回及第一層完整性摘要，預設統一使用 `scripts/run-candidate-backtest.sh`，操作與界線見 `doc/候選回測自動化.md`；不得再手工拼接 bundle ID、資料容器或輸出路徑，除非正在診斷腳本本身。Baseline 零差異控制重播必須使用 runner 的 `--control --candidate-id p3-z-baseline-control`，不得以未辨識的候選旗標搭配一般 `--record-decision-delta` 冒充控制組；App 未建立 Run／DecisionDelta 產物或畫面已顯示失敗時立即中斷，不等待 runner 長逾時。腳本一次只執行已核准的一個候選／一個樣本，不代表授權下一樣本、採用、commit 或 push。
- 全期間壓力測試在程式設計上不產生 DecisionBase／DecisionDelta；同一候選若已由固定三年窗口的 DecisionDelta 建立因果證據，可經使用者核准改做 score-only 全期間重播，不應為形式完整而先建不存在的全期間 DecisionBase。結果仍須核對 `.complete` 與 manifest 的 run ID、Sample、T/S、規則 commit、輸入、資金及加碼設定，並比較總分、股群 ROI、平均週期、資金不足與個股集中度；全期間只作風險證據，不取代固定窗口採納依據。
- 候選本身若推進 `T/S` 資料規則版本，不得直接讓一般候選 runner 以舊版正式 DecisionBase 等待差異完成：先分段建立候選資料版本的同規則 DecisionBase，以同版本 DecisionDelta 完成標記驗證輸出完整性，再將候選 `periods.csv` 與舊版正式 Baseline 逐位元或逐欄比較策略相容性。runner 若在候選完成後只因 DecisionBase 目錄版本不同而退出，應保留已完成產物並按上述方式判讀，不重新等待完整回測。
- 正式 Baseline 報告的 `.complete` 只在 manifest 所列報告檔、同次執行要求的 DecisionBase／DecisionDelta／反事實輸出及全期間瀏覽快照全部成功完成後才建立，內容為 run ID。自支援此標記的版本起，等待與搬回輸出必須同時核對 `.complete` 內容及 manifest 的 run ID／Sample／T/S／規則 commit，不以 App PID、畫面狀態或部分檔案存在判定完成；舊報告若產生於標記導入前，仍可依完整 manifest 與其所列報告檔個別驗證，不因缺少標記失效。
- 開始新實驗前，先閱讀 `doc/現行回測規則.md` 與 `doc/回測規則驗證.md`，並檢查 `exports/backtest-reports` 的目錄、`manifest.json`、`baseline.json` 與 `periods.csv`，確認目前 Baseline、現行條件、覆蓋狀態、輸入快照及既有實驗；不得只憑交談記憶猜測。大型 `.store` 只有在追查逐筆交易差異時才需讀取。
- 修改、重新解釋或考慮刪除 `tUpdate` 技術欄位前，先閱讀 `doc/技術數值與研究原則.md`。欄位尚未進入正式規則或用途仍待研究，不等於數值毫無意義；只要能穩定描述明確現象，就保留為候選研究材料，再用拆解回測判斷是否有效或重複。
- 自 `T2` 起，所有 `v` 欄位共用包含當日、僅限正式 TWSE 日資料的合格成交量觀察序列；Yahoo 盤中或歷史列不推進統計，當日 TWSE 一經取得便立即納入，`vMax9／vMin9` 亦遵循相同基準。
- Baseline 的股票、日期、分數等易變資料以 `doc/現行回測規則.md` 和對應 manifest 為準，不重複固定寫在本檔。
- 更換回測標的或重建快照時，沿用固定三年窗口原則：從正式模擬起始日開始，每隔三年建立一個不重疊的完整三年窗口；再加入一個以快照截止日倒推三年的「最近完整窗口」，即使與前一窗口重疊也保留。少於三年的殘餘期間不納入主分；另以最早模擬起始日至快照截止日建立單一全期間壓力測試，但不取代固定三年主分。技術資料應儘量包含最早模擬起始日前一年的準備期。
- 股票歷史與 `tUpdate` 固定輸入集中保存在有版本的共用資料池；資料池不隨每次 `simUpdate`／Baseline 版本推進而重播，只有準備重組股票樣本時，才以當時最新正式 S 規則更新期末 Grade 與適配趨勢。A／B／C／D／E 等 10 檔樣本由資料池建立有版本分片，各自保存固定輸入副本並產生獨立 Baseline；資料池中的舊 S 結果不得當成新版 Baseline。擴充或替換股票先更新資料池，既有樣本也不因候選結果中途換股。
- 非正式大盤資料比照集中資料池，只是本機研究與 Baseline 固定輸入，可快取、增量更新及建立有版本分片；來源提供欄位與現行 `tUpdate` 可類比統計應一次完整保存，不依 App 消耗先行刪減。它不是使用者 App 的正式資料庫。只有大盤候選通過完整回測並正式採用後，才另案讓 App 建立所需子集的獨立市場模型、下載、重算及遷移流程；研究資料池只能作公式與重疊日期的對照基準，不得冒充 App 資料已建立。細節見 `doc/大盤歷史資料與市場技術值前置計畫.md`。
- 大盤規則只能用決策當日及以前已知的市場狀態，描述現象後對 H買、L買、賣出或加碼提供一張小權重票，不把未來價格或模型預測寫入規則。研究優先直接檢驗「大盤現象 × 現行策略規則」與實際策略效率的交互，不再反覆把找回既有個股規則當成未知大盤研究的前置任務。同一大盤日跨股票只算一個市場日期群組；接線與資料正確性用零差異及人工植入樣例控制，未通過不解讀排名。歷史關聯只能形成一條候選，是否改善必須由完整 `simUpdate` 受控重播決定。細節見 `doc/大盤現象投票研究計畫.md`。
- Sample E 固定使用原候補 10 檔作為全弱勢壓力樣本，兩個各 5 檔的甲／乙組只用來維持既有計分尺度，不代表較強／較弱分層，也不得用 E 的絕對分數直接評斷 A／B／C／D 的均衡代表性。E 優先用於認賠、資金占用與持股週期研究；建立與重播沿用集中資料池既有行情及現行 T，不重新下載，仍須從每個窗口起點完整重播現行 S。這項研究優先順序不代表可跳過逐項實驗核准。
- `tUpdate` 產生的技術數值是回測的固定輸入。只修改買賣、賣出、加碼或 Grade 規則時，不重算 `tUpdate`；每個候選必須從窗口起點完整重跑 `simUpdate`。
- 原始輸入快照保持不變。Baseline 與每個候選都使用獨立副本，且必須使用相同股票、資料截止點、窗口、本金、加碼及交易成本；候選規則是唯一變因。
- 一次優先檢驗一個可解釋的假設。對已有明確現象意義的數值規則，一次只調一個門檻，以現值為中心向低、向高各建立候選並成對跑 Sample A；若鄰近步距兩側都零差異，可將同一門檻的距離放大一次找方向，不因第一個小步距無效就換門檻。只有一側呈現跨窗口且非單股主導的改善才視為方向成立，再往現值方向折半尋找較小有效改動，確認後送 Sample B；放大後仍無法辨別方向就保留現值並停止該門檻。複合規則逐一校準內部門檻，不共同移動多個門檻；整條移除只用於有理由判斷整條規則是否應退出時。觸發分布、逐條重疊或執行資格診斷不是預設前置工作，只有結果異常或無法解釋時才限定補做；避免網格式窮舉，二分只沿已由實際回測辨識的方向進行。
- 提出診斷、實驗或其他有成本的研究行動時，先列出實驗代號、唯一變因、固定條件、檢查項目、結果預期解鎖的下一項決策、正反結果各自的後續分支與停止條件，等使用者確認後才執行；每項實驗的完成回報同時列出下一候選的上述項目，方便使用者直接確認，但不得未經確認自動續跑。若本項結論是保留原規則且沒有深入價值，下一候選應直接移至下一條待驗規則，不只停在本門檻或同一規則，讓使用者可用簡短肯定繼續推進。若討論後找不到任何值得由結果驅動的後續行動，應縮小、改寫或取消本次研究。純探索仍可執行，但須明確標示為好奇導向並限定範圍，不把「沒有修改規則」誤判為失敗。
- Codex 列出下一個待核准的檢驗項目後，使用者回覆「好」或同義的簡短肯定，即表示確認執行所列項目；除非回覆另有限制、暫停或改變範圍，不再只當作收到資訊的確認。
- H 先判斷，H 不成立才評估 L；因此 H、Grade、賣出或加碼等上游規則有正式變更時，先建立並驗證新 Baseline，再檢驗下游規則。
- 分析與回報須明確區分「H／L 股群」（依選股評等形成的樣本分類）與「H／L 買入規則」（交易當日的追高／承低進場型態）。單寫 H／L 時只依緊接前文的單一語境解讀；同時涉及兩個維度時，明寫「H買／L買」及「H股群／L股群」，不得讓讀者自行猜測。股群分數不能直接當成該買入型態的績效，需要判斷交互效果時，應以「股群 × 買入規則」交叉拆解。下次建立新股票樣本時，優先將股群改用不與 H買／L買衝突的名稱。
- 分析不能只看 H+L 合計；至少比較 H、L、各固定窗口、ROI、平均持股週期及是否由少數股票或單一窗口造成。固定三年窗口是主要採納依據，全期間只作長期壓力測試，除非使用者另行定案。
- 回報候選的改善、退步或零差異時，必須明列比較對象，至少包含候選代號、正式 Baseline 的 Sample／版本或 run ID，以及雙方總分；有股群或窗口差異時也應並列雙方數值，不得只報差值。
- A／B／C／D 都只是固定且經設計選出的股票樣本；只要唯一變因、輸入與重跑方式受控，各樣本取得的正負分數都是有效實驗證據。即使四組都同向，也只能稱為「在目前特選樣本內較一致」，不得稱為規則穩定、普遍有效或可外推；換入任一新樣本仍可能反轉。另一樣本反向只能降低外推信心或顯示條件差異，不能倒推先前分數無效，也不得把「所有樣本與窗口不可反轉」設為自動否決門檻。採用時仍以固定三年為主，並依規則目的配置證據權重：以協助選中股票累積至 `wow` 為目的時，較強股群的改善優先於較弱股群，但較弱股群的資金占用、週期與大幅退步仍須明記為風險。A／B 方向相反且效果集中少數股票或窗口時，若現有 C／D 可用，先以相同唯一變因完成 C／D，再綜合幅度、股群與代價決定；只有四組仍無法分辨時，才標記待未來新樣本重驗。
- 分數制把每條簡單規則視為對局部現象的小權重投票，由方向不同的規則互補後再跨過交易門檻；不要求單一規則在 H／L股群、每個窗口與不同樣本全部獨立有效。邊際貢獻與互補必須由完整回測證據支持，不得掩飾過度擬合、資金風險、持續大幅退步或同義規則重複計分；設計與解釋原則見 `doc/現行回測規則.md#為什麼採分數制` 與 `doc/回測規則驗證.md#分數制規則的設計觀點`。
- 候選在任一主要比較維度（股群、合計或固定窗口）的分數絕對變化超過 1 分時，即使其他維度混合或小退，停止前也必須檢討改善與代價來源、現有規則是否實際互補，以及 Sample B、有因果關係的邊界拆解或限定交互診斷能否解鎖下一項決策。這是後續研究訊號，不是自動採納門檻或網格窮舉授權；跨維度一致性仍用於判斷採用證據強度。
- 診斷事件少但已有跨股票／窗口或跨樣本一致、機制可解釋的期待方向時，不因事件數少自動停止；先擴大現有固定樣本並分析有因果關係的 Grade／趨勢分段等合理界線。若只有單一案例、沒有比較基準或方向，應先標為待擴樣而非宣稱有效；方法本身無效或沒有任何決策關鍵事件時，則先修正方法或停止，不以增加樣本掩蓋問題。
- 從 `browse.store` 或其他逐日資料做診斷時，必須依 manifest 的模擬截止時刻裁切，不得把截止日後未參與該次 `simUpdate` 的行情列當成期末狀態；期末 ROI、平均週期、輪次及 Grade 應再與 `periods.csv` 核對。
- Grade 是每筆 Trade 依當時累積 ROI、週期與輪次動態形成的狀態，不把報告末端或目前顯示的個股 Grade 倒推為整段歷史評等；診斷若需要交易當下的 Grade，必須在當日模擬結果更新累積值之前直接擷取，不得由完成模擬後的同一列重新計算。若候選在目標股群（例如 H 規則對 H、L 規則對 L）改善，卻使另一股群相對退步，或改善明顯集中在較新窗口，可把交易當日 Grade 視為可能缺少的現象條件，依 `.none／.fine／.high／.wow` 的巢狀門檻或互斥 Grade 區段逐步拆解；這只是觸發檢驗的線索，不直接推定高 Grade 限制必然有效。若限制愈嚴效果持續縮小，或主要改善反而來自較早窗口，應停止繼續窮舉更高 Grade。
- Grade 級距已由回測證明可作為主要情境分類，但同一級距仍可能涵蓋寬廣的連續效率分數。當候選差異在相同交易當下 Grade 內仍呈混合、既有 Grade 邊界無法辨識方向時，可再讀取同一決策時點、更新當日累積結果前的連續 Grade 分數，依結果分布提出一個有因果關係的區間邊界（例如同為 `wow` 時比較 100 分以下與 100～200 分），再以單一變因完整重播。這是 Grade 內部的次級診斷，不取代正式級距、不使用期末分數倒推歷史，也不授權多門檻網格搜尋；相鄰界線仍遵循一次一門檻、由回測方向驅動及跨樣本確認的規則。
- `.none` 不是可描述策略適配強弱的正常 Grade。DecisionDelta 可把 `.none` 案例列入診斷與計分來源分析，但正式候選不得以 `.none` 作為納入條件、正向觸發或門檻邊界；只能明確排除 `.none`，再由 `.fine／.high／.wow` 或 `.weak／.low／.damn` 等有效 Grade 表達規則情境。
- 候選在 Sample A／B 方向反轉時，若 DecisionDelta 已顯示正負結果分別集中在可解釋的交易當日 Grade、窗口或現象分支，停止前必須先檢查能否保留正向分支並排除反向分支，形成一個最小、具因果關係的重組候選；只有找不到一致分界，或完整重播後仍反轉，才停止該方向。旁錄只用來形成重組假設，不能直接相加取代 A／B 完整重播。
- 第三個固定三年窗口另有「股群代表性的最後窗口」判讀用途：期末 Grade 落在 `.fine` 以上可支持該股仍呈現高股群方向，落在 `.weak` 以下可支持低股群方向，`.none` 則不作方向判定。若 H 股群在第三窗口對某候選特別好或壞，應檢查差異交易是否集中在 `.fine` 以上或 `.weak` 以下，再決定下一項實驗是否加重或避開該 Grade 方向；此訊號只用來形成假設，不是採用證據，正式條件仍須使用每筆交易當時的 Grade 回測。
- 現有樣本、尤其 Sample B 的代表性校正顯示，`.weak` 以下與較弱股群方向有明顯關聯；因此候選若改善較強股群、卻使較弱股群退步，而低 Grade 分支又幾乎不影響較強股群，可把「對 `.weak` 以下減少扣分或增加小幅正分」，以及再縮至 `.low／.damn` 的補償分支，列為有因果關係的下一步。這是以交易當日動態 Grade 對不同情境路由分數，不得把固定回測股群直接寫入正式規則；先以相鄰權重或邊界做有序比較，若較弱股群隨低端權重提高而改善、較強股群無實質損失，才視為方向證據並送不同樣本成對確認。細節見 `doc/回測規則驗證.md#grade-作為股群情境的分數路由`。
- 股群歸屬是建立樣本時固定的實驗設計，不隨候選或新版 Baseline 所產生的期末 Grade 中途改組。採用會改變動態 Grade 的上游規則並重建 Baseline 後，仍沿用原股群完成已規劃的規則檢驗；個股跨越 `.fine／.weak` 方向是需要記錄與分析的規則效果，不是立即調群或換股的理由。只有完成該檢驗階段、準備建立有版本的新樣本時才重新檢查代表性；若多數股票跨群而使原分層失去辨識力，則停止擴大檢驗並先共同決定是否提早換版。
- 不設自動採納門檻。每次提供「分析摘要」，由使用者與 Codex 共同決定採用、拒絕、保留原規則或待不同樣本重驗，必要時再拆條件或改參數。
- 現行效率分數已把 ROI 與平均持股週期合成為共同決策尺度；兩者交換價值沒有另經驗證的客觀換算時，候選採用判斷應以各固定樣本的分數方向與幅度為主，不得在看到結果後再用主觀 ROI／週期偏好覆蓋分數。單一樣本的小幅反向不是自動否決，須與其他樣本的正向幅度、案例集中度、資金不足及異常值一起判讀；若歷史筆記仍主張任一樣本退步即放棄，或另以未驗證權重重算 ROI／週期，應明確指出它與現行分數制及多樣本證據原則矛盾，待共同定案，不得默默沿用。
- 無成交案例不計分；遇到 0、Inf、NaN、零差異控制失敗或其他資料／程式異常時，不只停止或籠統標記失敗；先保留現場與第一分歧，區分輸入身分、接線／前態、候選預期路徑、runner／產物完整性及非決定性問題，完成根因診斷才擬定修復、改設計或停止。已核准任務內的可安全診斷與修復連續完成後重跑受影響控制；未恢復合格前不接受報告，也不解讀研究結果。
- 未採用的候選不得殘留在正式規則中。完成一批檢驗後，確認程式已回到最後採用的正式規則。
- 現行規則若已依核准的重驗程序完成停止條件並決定保留，應記為「重驗結案」，從可疑規則、待不同樣本重驗與後續候選清單移除；即使證據是局部有效加上其他樣本無代價，也不因未達全面普遍性而反覆重開。文件仍須明記證據範圍；只有股票樣本、窗口、Baseline、上游規則或資料正確性發生足以改變判讀的實質變化，或出現新的反向證據時，才可提出新的重驗理由。
- 候選回測不需逐次更新固定瀏覽環境。13 吋 Simulator 預設保留目前正式 Baseline 的可瀏覽副本；只有正式 Baseline 更新時，才同步重建並安裝瀏覽副本、開啟 App 並保持 Simulator 開機。除非使用者明確指定，否則不以暫時候選取代固定 Baseline 瀏覽資料。

## 回測紀錄

- 標準化回測決策旁錄的格式、去重、保存與分段實作以 `doc/回測決策旁錄與差異資料設計.md` 為權威；正式固定窗口 Baseline 建立或規則 commit 改變時，應同步評估重建 Sample A／B DecisionBase。規則目錄須完整列出全部正式規則，即使某 Sample 零觸發也不省略；事件只保存非零票，候選只保存相對正式基底的差異，不重建完整 DecisionBase。零差異候選不得建立空 delta SQLite；第一次分歧前狀態不一致時停止使用該次結果。
- 適配趨勢來自 Grade 背後的連續效率時間序列，屬於策略內生的 `simUpdate` 狀態，不得寫入 `tUpdate`。研究與交易規則導入依 `doc/適配趨勢研究與導入計畫.md` 分階段執行；S22 起的預警／確認／確認後退回階段另依 `doc/Grade趨勢預警狀態持久化計畫.md` 每筆滾動保存。同一交易日 Yahoo 重算只使用前一交易日階段與目前值，不記憶盤中曾經經過的路徑；資料池未完整重播前不得誤標為已完成最新 S，未經正式回測採用的交易規則也不得讀取顯示用途階段。
- Candidate delta 完成後應同步產生 P4a 標準分析，明確分開直接票／gate 差異與後續路徑連鎖，並以第一次分歧連到 Grade、窗口、股群、個股及期末結果。規則的分數跨門檻次數與唯一通過 gate 只是局部決策指標，不得直接稱為規則的因果績效貢獻；完整因果仍須限定反事實重播。
- P5 限定反事實只對已核准的單一事件執行：先用同一 event key 與預期 state fingerprint 跑零變更控制，確認精確命中一次、決策事件與期末結果全零差異後，才可增減一票或改變一次行動並從窗口起點完整重播。fingerprint、事件鍵、正常行動或命中次數任一不符即停止；結果只稱該事件的局部邊際效果，不倒推為整條規則的完整貢獻。
- DecisionBase 完成後應同步產生 P4b 規則重疊矩陣與決策分數相鄰門檻清單。規則共現、Jaccard 或單向涵蓋率只能形成重複性檢查線索，不能直接判定規則重複；只有旁錄已保存原始觀察值時，才可生成逐條技術條件的參數臨界值，不得由規則名稱或描述猜造。
- Swift 程式只保存穩定規則代號與簡短目的；詳細分數、run ID、判讀與採用理由集中記錄在 `doc/回測規則驗證.md`。
- 正式規則使用 `G-P/N/T/M`（Grade）、`H-P/N/C/T`、`L-P/N/C/T`、`S-P/N/T`、`A-P/N/T/E`（加碼）與 `E-nn`（固定成交／資金假設）；退出規則使用各類別的 `R` 代號。既有代號不得因刪除或插入規則而重編，可獨立拆解的分支使用 `a／b` 後綴。`H1`、`L6b`、`S1a`、`A5d` 等只是實驗代號。
- `exports/backtest-reports/<run-id>/` 保存本機詳細報告與重現資料，預設不納入 Git。每次實驗應保留可辨識的 run ID、`Tn/Sn` 資料規則版本、策略規則版本、輸入快照、設定與資料品質資訊。
- 使用者所稱的「報告」是 `exports/backtest-reports/<run-id>/report.html` 回測實驗報告，不以驗證文件摘要或單獨建立 `baseline.json` 取代。形成新 Baseline 時，報告必須以上一版與新版 Baseline 比較，最前方提供足以直接判讀的「分析摘要」，其後再列股群、窗口與個股分項數據供疑點追查。
- 每份 Sample B Baseline 報告都要在分析摘要或緊接其後的獨立短段落，精簡比較相同資料／策略規則與窗口下的 Baseline A；跨樣本高低只用來描述樣本敏感度與差異集中處，不稱為規則改善或退步。比較解讀還須說明策略規則（尤其 S 賣出規則）應讓適合策略的股票累積出 `wow` 績效、把低效率股票辨識至 `weak` 以下，以及 Sample B 是代表性驗證樣本而非推薦買進清單。
- `doc/現行回測規則.md` 是正式 Baseline、股票樣本、精確現行條件及覆蓋進度的權威清單；每條正式規則須記錄未檢驗／整組檢驗／部分拆解／已獨立檢驗狀態，以及最後相關實驗。`doc/Baseline歷史.md` 集中保存歷次正式對照基準的規則變更、同樣本前版分數差異及 A／B 固定三年與全期間報告連結；`doc/回測規則驗證.md` 只保存驗證方法與計畫、歷史結果、採用決策及已退出規則，不重複現行清單或歷史索引。正式採用新 Baseline 時，Codex 應自行更新現行文件中的 run ID、精確規則 commit、`Tn/Sn` 資料規則版本、策略規則版本、輸入截止日、窗口、股票群、資金／加碼設定與主分，同步追加 Baseline 歷史並讓 HTML 報告明寫本版規則變更，不必等待使用者另行提醒；暫時候選、UI 或文件修改不更新 Baseline。
- 同一批實驗不必逐項修改驗證文件；先保存報告與暫定結論，在形成新 Baseline、準備 commit 或完成一個檢驗階段時再批次整理。
- 一般候選檢驗只跑足以判讀的主要固定窗口，並在交談提供分析摘要；現有回測器為取得結構化結果而順帶產生的暫存 HTML 可以保留在 Simulator 容器，但不逐項複製、保存或交付至專案 `exports`，也不逐項跑全期間壓力測試。只有建立新 Baseline，或使用者特別要求時，才在專案中產生正式固定窗口與全期間完整報告；候選即使完成整批或可能影響正式 T/S 規則，也須等採用並進入 Baseline 流程後才建立正式報告。
- 更換股票群、增加新的完整窗口、修正歷史／技術／模擬錯誤，或實質改動 Grade、賣出、加碼等上游規則時，應評估重驗既有結論；一般 UI 修改或單純更新 App 版本不需重驗。
